#include "../linkedList.c"
#include <stdio.h>

int main(){
    Node* head1 = (Node*)malloc(sizeof(Node));
    head1->data = 0;

    Node* head2 = (Node*)malloc(sizeof(Node));
    head2->data = 0;

    int i = 2;

    while (i!=-1){
        scanf("%d", &i);
    }

}